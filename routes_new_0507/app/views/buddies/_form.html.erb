<%= form_for(@buddy) do |f| %>
  <% if @buddy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buddy.errors.count, "error") %> prohibited this buddy from being saved:</h2>

      <ul>
      <% @buddy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :breed %><br>
    <%= f.text_field :breed %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
